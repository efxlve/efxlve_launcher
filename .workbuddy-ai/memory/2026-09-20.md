# 2026-09-20 — Efxlve Launcher

## Üst bar (titlebar) yeni nesil yeniden tasarım
- `index.html` üst bar iskeleti yenilendi: yükseklik 42px → **56px**, marka (`.logo-mark` + `.logo-text`), segment nav (`.nav-seg` + `.nav-seg-indicator`), bağlantı rozeti (`.net-chip`), hesap çipi (`.account-chip`), ayarlar ikon düğmesi (`.nav-icon-btn`).
- `src/styles.css`: titlebar bloğu komple yeniden yazıldı; ölü `.offline-toggle-btn` kuralları silindi. Responsive eşikler: ≤1120px hesap adı gizli, ≤940px ikon-only güvenlik ağı.
- `src/main.ts`:
  - `updateNavIndicator()` eklendi — kayan aktif sekme göstergesi; `render()` ve `handleWindowResize()` içinden çağrılıyor.
  - `updateOfflineModeUi()` artık innerHTML yazmıyor, sınıf + etiket metni güncelliyor.
  - `updateChrome()` hesap çipi için `dataset.acct` önbelleği kullanıyor (createIcons innerHTML'i svg'ye çevirdiği için naif karşılaştırma sonsuz döngü yapıyordu).
  - `Ctrl+1/2/3/,` klavye kısayolları eklendi.
- Doğrulama: `npm.cmd run build` temiz; Edge headless ile 1024/1120/1280px genişliklerde taşma ölçüldü (taşma yok).
- Commit: `f4a278a`. AGENTS.md §71 eklendi.

### Yeniden kullanılabilir yöntem (görsel doğrulama)
Bu makinede Playwright yok; tasarım doğrulaması **Edge headless** ile yapılıyor:
`"C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe" --headless=new --disable-gpu --hide-scrollbars --force-device-scale-factor=2 --screenshot=out.png --window-size=1280,300 --user-data-dir=<tmp>/ud "file:///.../preview.html"`
- `--window-size` ile verilen genişlik, `window.innerWidth`'ten ~30px büyük çıkar (başlık çerçevesi payı).
- Statik önizleme için `dist/assets/index-*.css` doğrudan link edilir; lucide `createIcons` çalışmadığı için ikonlar inline SVG olarak gömülmelidir.
- Taşma ölçümü: önizlemeye `nav.scrollWidth > nav.clientWidth` yazan küçük bir script eklenip ekran görüntüsünden okunur.

## Üst barın "AI tasarımı" klişelerinden arındırılması (2. tur)
- Tetikleyen geri bildirim: *"ai sıçmış gibi, tüm ai tasarımlar böyle"* — yani gradyan + glow + kapsül üçlüsü.
- Kaldırıldı: radyal mor/cyan arka plan parlamaları, `::after` gradyan çizgi, gradyan logo + hover dönme, gradyan kayan gösterge + mor glow, `net-pulse` nabız, `nav-wiggle`/`nav-pulse`/`nav-drop`, ayarlar ikonunun 38° dönmesi, gradyan avatar, rozet pop/glow.
- Yeni dil: düz 2 duraklı koyu bar, 1px hairline, yükseklik **48px**, çukur segment zemini + düz `#24262f` aktif karo, yarıçaplar **8/6px**, kapsüller 6px dikdörtgen, metinler nötr gri. **Renk sadece durum bildirir** (yeşil `#3fb950` / amber `#d29922` nokta), tek marka vurgusu logo ikonunda düz `#9d94e8`.
- Rozet: absolute köşe rozeti "İndirmeler" metnine biniyordu → **satır içi** sayaca çevrildi. Satır içi olduğu için sekme genişliği değişiyor → `updateBadge()` sonuna `updateNavIndicator()` eklendi (aksi halde kayan karo bayat kalıyor).
- Responsive eşikler yeni ölçülere göre: hesap adı `≤1060px`, ikon-only `≤880px`. 1024px'te seg=326 + right=173, 246px boşluk.
- Commit: `6028c40`. AGENTS.md §3'e zorunlu "AI tasarımı gibi durmasın" kuralı + §72 eklendi.

### Kullanıcı tercihi (kalıcı)
- Kullanıcı AI-üretimi görünen tasarım klişelerine karşı hassas: gradyan, neon glow, gradyan metin, her şeyi kapsül yapmak, süs amaçlı mikro animasyon. Sade/düz/ölçülü tercih ediliyor. (AGENTS.md §3'te kural olarak sabitlendi.)


## Üst bara karakter kazandırma (3. tur — "ruhsuz/kütük" düzeltmesi)
- §72'deki aşırı sadeleştirme kullanıcı tarafından *"çok ruhsuz, kütük duruyor"* bulundu.
- **Ders:** AI klişelerini sökmek ≠ ruhsuz bırakmak. Karakter gradyandan değil; **yüzey dilinden, tipografiden, derinlikten ve tek özgüvenli vurgu renginden** gelir.
- Uygulananlar:
  - **Tuş (keycap) yüzey dili:** tüm etkileşimli öğeler düz dolgu `#14161d` + 1px kenarlık + `inset 0 1px 0 rgba(255,255,255,0.045)`; basınca `translateY(1px)`.
  - **Gerçek derinlik:** `#titlebar` içeriğe gölge düşürüyor (`0 14px 26px -20px #000`), segment yuvası çukur (`inset 0 2px 5px -1px rgba(0,0,0,0.85)`). Glow değil, gölge.
  - **Tek vurgu rengi `#7f6ce8`, hep düz, sadece 3 yerde:** aktif sekme alt çizgisi (`.nav-seg-indicator::after`), `.logo-mark` ikonu, girişliyken avatar baş harfi + kenarlığı (+ aktif ayarlar ikonu).
  - **Tipografik ses:** sekmeler UPPERCASE 11px/700/ls 0.95px. `lang="tr"` sayesinde "İNDİRMELER" doğru büyüyor.
  - **Durum ışığı:** sabit LED halosu (`0 0 0 3px rgba(...,0.14)`), nabız yok.
  - **Tek hareket:** kayan karo `cubic-bezier(0.34, 1.22, 0.64, 1)` hafif overshoot.
  - Bar 48 → 50px.
- Ölçüm: 1024px'te seg=355 + right=171 (216px boşluk) — taşma yok. Eşikler değişmedi.
- Commit: `55a09b9`. AGENTS.md §73.

### Üst bar üzerinde çalışırken uyulacak denge (kalıcı)
İki uçtan da kaçın: (a) gradyan+glow+kapsül "AI" görünümü, (b) hiç vurgu/derinlik olmayan düz gri "kütük" görünüm. Hedef: sade ama karakterli — düz renkler, ölçülü yarıçaplar, tek vurgu rengi, işlevsel hareket.

## Üst barın PS5 diline taşınması (4. tur — "hâlâ kütük" düzeltmesi)
- Kullanıcı §73'ü de *"hâlâ düz, kütük gibi"* buldu ve **PlayStation tasarımından esinlenmeyi önerdi**.
- **Teşhis:** PS5'in ruhu KUTUDAN değil IŞIKTAN gelir. §73'te her öğeyi keycap kutusuna sarmak barı "panel yığını"na çevirmişti. PS5 neredeyse hiç kutu kullanmaz; öğeleri ışık + çizgiyle ayırır.
- **Uygulananlar:**
  - Segment yuvası ve aktif karo tamamen kaldırıldı — `.nav-seg` sadece 20px boşluklu flex grubu.
  - **Bağlamsal ortam ışığı:** `@property --nav-ambient` (syntax `<color>`) + `#titlebar::before` tek radyal yıkama. `updateNavAmbient()` bölüme göre renk atar (mağaza mavi, kütüphane menekşe, indirmeler turkuaz, profil altın, ayarlar slate). `@property` sayesinde 0.55s'de yumuşak geçiş.
  - **Odak parıltısı:** `.nav-tab::before` radyal beyaz ışık (hover %75, aktif %100). Kutu değil, ışık.
  - **Kayan beyaz alt çizgi:** `.nav-underline` artık `#titlebar`'ın doğrudan çocuğu. JS `offsetLeft` yerine `getBoundingClientRect()` farkı kullanıyor.
  - İkonlar 18px / `stroke-width: 1.7`; sekmeler 11px/700/ls 1.1px uppercase; aktif ikon `scale(1.12)`.
  - Sağ küme yarı saydam PS5 kontrol merkezi döşemesi (ortam ışığını geçirir). Bar 54px.
- Ölçüm (uzun adla en kötü durum, DOM dump): vw=1024 → nav=736, right=181, taşma yok; vw=1280 → right=296, taşma yok. Eşikler: ad `≤1200px` gizli (`max-width: 150px`), ikon-only `≤960px`.
- Commit: `9ab7659`. AGENTS.md §3'e **ortam ışığı istisnası + denge kuralı**, yeni §74.

### Öğrenilen tasarım ilkesi (kalıcı)
Karakter dört kaynaktan gelir: (1) **bağlamsal ışık** (içeriğe göre değişen tek kaynaklı ambient), (2) **yüzey/derinlik dili**, (3) **tipografik ses**, (4) **tek özgüvenli vurgu rengi**. Kutu sayısını artırmak karakter katmaz — kutular çoğaldıkça "panel yığını / kütük" hissi artar. PS5 bu yüzden neredeyse hiç kutu kullanmaz.

### Önizleme aracı notu
Edge headless ekran görüntüsü, `--user-data-dir` aynıysa ESKİ CSS'i önbellekten kullanabiliyor → yanıltıcı sonuç. Her ölçüm/görsel için **temiz bir `--user-data-dir`** kullan ya da `--dump-dom` ile ölç. CSS dosya adı her derlemede değiştiği için mock'lardaki `index-*.css` bağlantısını da güncellemeyi unutma.


## 5. Tur: Dört tasarım turunun yan yana karşılaştırması

**Talep:** *"ikisinin karşılaştırmasını görsek bi ha"* → ikisini değil **dördünü** yan yana koyma kararı alındı.

**Yöntem:** Her turun gerçek `styles.css` + `index.html`'i commit'ten çıkarıldı (`f4a278a` 56px, `6028c40` 48px, `55a09b9` 50px, `9ab7659` 54px). Çıkarım aralığı `/* --- Titlebar` → `\n#content {` (bu aralık pencere kontrol butonlarını ve üst bara ait `@media` kurallarını da alır). `:root` ayrı, lucide ikonlar satır içi SVG'ye çevrildi. Her sürüm ayrı bir HTML olarak render edilip PNG alındı, sonra base64 ile tek karşılaştırma sayfasına gömüldü (iframe **çalışmadı** — headless'ta boş kaldı).

**Sonuç:** `karsilastirma.png` — 1400×674 CSS (2800×1348 px, 2x). Betik: `%TEMP%\efx-prev\build_compare.py`.

**Kritik doğruluk kararı:** karşılaştırma **1280px**'te yapılmalı (uygulamanın varsayılan pencere genişliği). 1080px'te `@media (max-width:1200px)` hesap adını gizliyor ve 4. turu *tasarım dışı* bir sebeple farklı gösteriyordu.

**Headless dersleri (AGENTS.md §7.2'ye işlendi):**
- `--dump-dom` koşusunda `innerWidth = 0` → `@media` yanlış değerlendirilir, sekme genişliği 40px ölçülür. Ölçüm ve görüntü **ayrı koşularda** alınmalı; `--virtual-time-budget` de `--dump-dom` ile viewport'u bozuyor.
- JS ile konumlandırılan gösterge ilk karede yanlış hesaplanıyor (viewport 0 → sonra gerçek genişlik). Çözüm: `resize` + `document.fonts.ready` + ~120 karelik `requestAnimationFrame` döngüsünde tekrar konumlandır; görüntü öncesi `*{transition:none !important}`.
- `offsetLeft` yerine `getBoundingClientRect()` farkı (containing block'un `border-left`'ını çıkar) → ölçek faktöründen etkilenmiyor.
- Belirti: v1'in aktif çizgisi "Mağaza" altında görünüyordu; sebep yukarıdaki iki maddeydi, CSS değil.

**Karşılaştırmanın okunması:** 1. tur "fazla AI", 2. tur "fazla ruhsuz", 3. tur "kutularla çözmeye çalışıyor", 4. tur "ışık ve çizgiyle çözüyor". §3 DENGE KURALI'nın görsel kanıtı.

**Açık kalan soru (kullanıcı yanıtlamadı):** ortam ışığı daha belirgin yapılsın mı? (şu an `rgba(...,0.18–0.22)` alfa).
